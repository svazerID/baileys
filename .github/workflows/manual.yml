name: Extract baileys.zip
on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  extract:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          token: ${{ secrets.TOKEN }}
      
      - name: Extract baileys.zip
        run: |
          unzip baileys.zip -d ./
          
      - name: Commit & Push hasil extract
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add .
          git commit -m "Extract baileys.zip" || echo "No changes to commit"
          git push
